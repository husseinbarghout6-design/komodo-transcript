services:
  - type: web
    name: komodo-transcript
    env: docker
    plan: free
    branch: main
    healthCheckPath: /health
    healthCheckTimeout: 30   # ⬅ raise from 5s → 30s
    healthCheckInterval: 10s
    healthCheckGracePeriod: 120s
    autoDeploy: true
